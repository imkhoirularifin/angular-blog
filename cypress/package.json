{
	"name": "cypress",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"cypress:open": "cypress open",
		"cypress:report": "cypress run --reporter mochawesome && npx mochawesome-merge cypress/results/*.json -o mochawesome.json && npx marge mochawesome.json && rm -r cypress/results && rm mochawesome.json"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"cypress": "^13.6.4",
		"mochawesome": "^7.1.3",
		"mochawesome-merge": "^4.3.0",
		"mochawesome-report-generator": "^6.2.0"
	}
}
